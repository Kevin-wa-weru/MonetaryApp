class Payments {
  final String id,
      date,
      amount,
      paymentcode,
      description,
      name,
      phone,
      paymentref,
      paymentstatus;

  Payments(this.id, this.date, this.amount, this.description, this.paymentcode,
      this.name, this.phone, this.paymentref, this.paymentstatus);
}
